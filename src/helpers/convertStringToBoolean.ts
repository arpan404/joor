/**
 * Converts a string to a boolean value.
 *
 * @param value - The string to convert. Should be 'true' or 'false'.
 * @returns `true` if the input string is 'true', otherwise `false`.
 */
const convertStringToBoolean = (value: string): boolean => value === 'true';

export default convertStringToBoolean;
